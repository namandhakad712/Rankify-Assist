# Requirements for MCP Server ONLY (browser/device control)
# Use this for: ONLINE-fastmcp.cloud_server.py

# FastMCP framework (compatible with Cloud version 2.12.3)
fastmcp>=2.12.3

# HTTP client for API calls
httpx>=0.25.0

# Environment variable management
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0

# Legacy/optional (for local testing)
requests>=2.31.0

# NOTE: tuya-mcp-sdk is NOT needed for MCP server!
# It's only needed for Tuya client (separate deployment)
